# Worker app: long-polling Telegram bot, no public HTTP service.
app = "my-agent-mem0"
primary_region = "iad"

[build]

[env]
  SCHEDULE_DB_PATH = "/data/schedules.db"

[mounts]
  source = "data"
  destination = "/data"

[[vm]]
  size = "shared-cpu-1x"
  memory = "512mb"
